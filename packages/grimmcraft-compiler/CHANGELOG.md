# Changelog — grimmcraft-compiler

No package-level changes. Benefits from workspace-wide updates in the root `CHANGELOG.md`: `fix(env)` (editable imports now work) and `chore(pytest)` (`--import-mode=importlib`, which lets this package's `tests/test_diagnostics.py` coexist with the redstone package's same-named test file).
