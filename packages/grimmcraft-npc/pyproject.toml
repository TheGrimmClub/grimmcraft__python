[project]
name = "grimmcraft-npc"
version = "0.1.0"
description = "Branching NPC dialogue (Ren'Py style) and Taterzens NPC presets"
readme = "README.md"
authors = [
    { name = "Andreas **Felix** Häberle", email = "felix@42sol.eu" }
]
requires-python = ">=3.12"
dependencies = [
    "grimmcraft-core",
    "grimmcraft-control",
    "grimmcraft-compiler",
    "grimmclub-filesystem",
    "grimmclub",
    "click>=8.1",
    "rich>=13.0",
]

[project.scripts]
# grimmcraft-npc = "grimmcraft_npc.cli:main"  # CLI comes with the Taterzens work

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
# Package sources live under srcs/ (not the uv default src/).
module-root = "srcs"

# Resolve sibling packages from the local workspace instead of an index.
[tool.uv.sources]
grimmcraft-core = { workspace = true }
grimmcraft-control = { workspace = true }
grimmcraft-compiler = { workspace = true }
grimmclub-filesystem = { workspace = true }
grimmclub = { workspace = true }
